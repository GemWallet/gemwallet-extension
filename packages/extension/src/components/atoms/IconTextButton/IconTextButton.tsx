import { IconButton } from '@mui/material';
import { styled } from '@mui/material/styles';

export const IconTextButton = styled(IconButton)(() => ({
  borderRadius: '7px',
  display: 'flex',
  alignItems: 'center',
  padding: '4px'
}));
